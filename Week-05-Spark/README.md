### Brief Insights
### Q3 
I removed the duplicate records using `tid` and `dt`.

### Q4
I filtered the Europe data and found the average revenue for each category.

### Q5
I filled the null values in the `status` column with `"Unknown"`.

### Q6
I didn't get any output because no region has more than 100 records.

### Q8
I filtered the data and got only the Premium users whose age is between 18 and 30.

### Q10
I changed the date column to timestamp format and renamed it as `event_time`.

### Q12
I checked for null emails and empty usernames. Since my data didn't have them, no rows were removed.

### Q13
I used `.agg()` to find the minimum, maximum, and average price.

### Q15
I removed duplicate records, filled null prices with `0`, and calculated the total revenue for the store.

